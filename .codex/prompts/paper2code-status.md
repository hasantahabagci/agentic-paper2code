Report the state of every paper reproduction in this project. Do not run any
stage.

1. List the workspaces under `.p2c/` — one subdirectory per paper.
2. For each, read `STATE.md` and report the paper title, the stage table, the
   next action, and any blockers.
3. Check the state file against reality rather than trusting it: do the
   artifacts of every `done` stage exist, does `tasks.md` agree with
   `STATE.md`, does the output repository contain the files stage 6 claims?
4. Report discrepancies and correct `STATE.md` to match disk.

Finish with the number of entries in `open-questions.md` and, if stage 8 has
run, the fidelity score from `review.md`.
